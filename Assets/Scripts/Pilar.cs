using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pilar : InteractableObjects
{/*
    [SerializeField]
    LampPuzzle _lp;
    [SerializeField]
    GameObject _lamp;

    bool _isUsed;

    public enum LampID
    {
        Lamp1,
        Lamp2,
        Lamp3,
        Lamp4,
        Lamp5
    }

    LampID thisLampID;

    public override void Interact()
    {
        if (!_isUsed)
        {
            if (_gm.RemoveLamp())
            {
                _isUsed = true;
                _lamp.SetActive(true);
            }
        }
        else
        {
            _lp.ActivateLamp((int)thisLampID);
        }
    }*/
}
